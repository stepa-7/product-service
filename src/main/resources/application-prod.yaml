spring:
  jpa:
    hibernate:
      ddl-auto: validate

  liquibase:
    enabled: true

  kafka:
    admin:
      auto-create: false
    properties:
      auto.create.topics.enable: false

logging:
  level:
    root: WARN
    org.productservice: INFO